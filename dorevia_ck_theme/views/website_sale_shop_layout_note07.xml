<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Note 07 · Lot A — Boutique pleine largeur + drawer filtres.
         Héritages xpath légers sur website_sale.products (priority 40).
         Contrainte : form.js_attributes conservé dans l'offcanvas natif Odoo —
         ne pas déplacer ni réécrire le formulaire de filtrage. -->
    <template id="products_ck_note07_layout"
              inherit_id="website_sale.products"
              name="CK Shop Note07 — grille pleine largeur + offcanvas desktop"
              priority="40">

        <!-- A1 : sidebar masquée — l'offcanvas natif contient son propre form.js_attributes -->
        <xpath expr="//aside[@id='products_grid_before']" position="attributes">
            <attribute name="class" add="d-none" separator=" "/>
        </xpath>

        <!-- A3 : offcanvas panel accessible sur desktop (retrait d-lg-none natif Odoo) -->
        <xpath expr="//div[@id='o_wsale_offcanvas']" position="attributes">
            <attribute name="class" remove="d-lg-none" separator=" "/>
        </xpath>
    </template>
</odoo>
