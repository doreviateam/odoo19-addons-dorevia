<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Phase 4 — composition fiche produit portable (install / -u dorevia_ck_theme). -->
    <delete model="ir.ui.view" search="[('key', '=', 'dorevia_ck_theme.product_ck_chips')]"/>

    <template id="product_ck_compose"
              inherit_id="website_sale.product"
              name="CK Product Phase 4 compose"
              priority="25">
        <!-- Passerelle Pro sous la zone longue (product_ck_long_zone) — pas de grande section ici. -->
    </template>
</odoo>
